# ============================================
# ⚠️  GÜVENLİK UYARISI - LÜTFEN OKUYUN!  ⚠️
# ============================================
# Bu dosya sadece ŞABLON amaçlıdır!
# Kullanım için:
#   1. Bu dosyayı .env olarak kopyalayın: cp .env.example .env
#   2. .env dosyasındaki placeholder'ları gerçek değerlerle değiştirin
#   3. Dosya izinlerini güvenli yapın: chmod 600 .env
#   4. .env dosyasını ASLA git'e eklemeyin!
# ============================================

# ============================================
# ORTAM AYARI (development / production)
# ============================================
ENVIRONMENT="production"

# ============================================
# MikroTik bağlantı ayarları
# ============================================
MIKROTIK_HOST="your_mikrotik_ip"
MIKROTIK_PORT=8728
MIKROTIK_USER="your_mikrotik_username"
# NOT: MikroTik şifresi şifrelenmiş olarak veritabanında saklanıyor
# İlk kurulumda buraya girin, sonra veritabanından yönetilir
MIKROTIK_PASSWORD="your_strong_password_here"
MIKROTIK_USE_TLS=True

# ============================================
# VERİTABANI AYARLARI
# ============================================
# PostgreSQL (Production - Önerilen)
DATABASE_URL="postgresql+asyncpg://wg_user:CHANGE_THIS_PASSWORD@localhost/wg_manager"
# SQLite (Development - Yedek)
# DATABASE_URL="sqlite:///./router_manager.db"

# ============================================
# JWT GÜVENLİK AYARLARI
# ÖNEMLİ: SECRET_KEY'i production'da değiştirin!
# Yeni key oluşturmak için: python3 -c "import secrets; print(secrets.token_hex(32))"
# ============================================
SECRET_KEY="CHANGE_THIS_TO_RANDOM_64_CHAR_HEX_STRING"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================
# Rate Limiting (DDoS Koruması)
# ============================================
RATE_LIMIT_PER_MINUTE=200
RATE_LIMIT_LOGIN=5

# ============================================
# Güvenlik Ayarları
# ============================================
ENABLE_HTTPS_REDIRECT=True
TRUSTED_HOSTS="localhost,127.0.0.1,your_domain.com"
MAX_REQUEST_SIZE=10485760

# ============================================
# Log ayarları
# ============================================
LOG_LEVEL="INFO"
LOG_FILE="logs/app.log"

# ============================================
# CORS ayarları (virgül ile ayrılmış liste)
# ÖNEMLİ: Production'da sadece gerçek domain'lere izin verin!
# ============================================
CORS_ORIGINS="https://your_domain.com"
